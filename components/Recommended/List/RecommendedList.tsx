import React from 'react';
import { BlockWrapper } from '../../BlockWrapper/BlockWrapper';

type Props = {};

export const RecommendedList = (props: Props) => {
  return <BlockWrapper>RecommendedList</BlockWrapper>;
};
